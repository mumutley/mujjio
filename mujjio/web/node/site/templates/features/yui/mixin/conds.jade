mixin dialog
  .dialog
    h1 whoop
    p #{data.principle}
    if data.principle == 'anonymous'
      p world hello //this indent is important to make a block
    p #{(data.principle == 'anonymous')}
